/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
        domains: ['flowbite.s3.amazonaws.com', 'fakestoreapi.com', 'i.dummyjson.com', 'dummyjson.com', 'www.themealdb.com'],
      },
}

module.exports = nextConfig
